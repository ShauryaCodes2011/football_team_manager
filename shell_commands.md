pwd                                         #get directory
ls                                          #list the files
python -m venv {virtual env name}           #creats a virtual env 
python3                                     #starts python
pip freeze                                  #list all installed packages
source {virtual env name}/bin/activate      #activates virtual env
deactivate                                  #turns off the virtual env